rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    function isAuthenticated() {
      return request.auth != null;
    }

    function getUserEmail() {
      return request.auth.token.email != null ? request.auth.token.email.lower() : '';
    }

    function isApprovedUser() {
      return isAuthenticated() && (
        getUserEmail().matches('.*@studiotunnel[.]com$') ||
        getUserEmail() in [
          'samiran@studiotunnel.com',
          'yash@studiotunnel.com',
          'sujith@studiotunnel.com',
          'sujithnair991@gmail.com',
          'manoj@studiotunnel.com',
          'contactmanojsahu@gmail.com',
          'tamash@studiotunnel.com',
          'ansarialtamash04@gmail.com',
          'tamashansari4@gmail.com',
          'prakash@studiotunnel.com',
          'prakashjai.tunnel@gmail.com',
          'arjun@studiotunnel.com',
          'arjuns825@gmail.com',
          'arjunkohli@gmail.com',
          'ayush@studiotunnel.com',
          'dalviayush10@gmail.com',
          'ayushdalvi@gmail.com',
          'vijay@studiotunnel.com',
          'vijaynool17@gmail.com',
          'vijaynool@gmail.com',
          'golu@studiotunnel.com',
          'golu.tunnel@gmail.com',
          'aaditya@studiotunnel.com',
          'aadikamble11@gmail.com',
          'aaditya.tunnel@gmail.com',
          'lab@studiotunnel.com'
        ]
      );
    }


    // Master Studio Administrators
    function isAdmin() {
      return isAuthenticated() && (
        getUserEmail() in [
          'samiran@studiotunnel.com',
          'yash@studiotunnel.com',
          'lab@studiotunnel.com'
        ]
      );
    }


    // Team Chat Attachments (max 50MB)
    match /artifacts/{appId}/public/chat/{fileName} {
      allow read: if isApprovedUser();
      allow write: if isApprovedUser() && request.resource.size < 50 * 1024 * 1024;
      allow delete: if isAdmin();
    }


    // Long Format / Project Attachments (max 100MB)
    match /artifacts/{appId}/long_format_files/{fileName} {
      allow read: if isApprovedUser();
      allow write: if isApprovedUser() && request.resource.size < 100 * 1024 * 1024;
      allow delete: if isApprovedUser();
    }

    // Default deny
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
