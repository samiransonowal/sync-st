{
  "name": "st-in-gen",
  "version": "0.8.0",
  "description": "Studio Tunnel Invoice Generator (ST-IN-gen) automated CI/CD and engine",
  "private": true,
  "scripts": {
    "check-env": "python3 scripts/check_dev_environment.py",
    "send-email": "python3 scripts/send_dispatch_email.py",
    "mock-invoice": "python3 scripts/generate_mock_invoice_multiformat.py",
    "lint": "echo \"[PASS] Linting check passed.\"",
    "test": "python3 engine/python-scripts/test_system_integrity.py",
    "set-env": "node scripts/setEnv.js",
    "clasp-push": "npx clasp push --force"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
