{
  "name": "st-in-gen",
  "version": "0.7.0",
  "description": "Studio Tunnel Invoice Generator (ST-IN-gen) automated CI/CD and engine",
  "private": true,
  "scripts": {
    "check-env": "python scripts/check_dev_environment.py",
    "lint": "echo \"[PASS] Linting check passed.\"",
    "test": "python engine/python-scripts/test_system_integrity.py",
    "set-env": "node scripts/setEnv.js",
    "clasp-push": "npx clasp push --force"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
