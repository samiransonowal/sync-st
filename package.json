{
  "name": "sync-st",
  "version": "1.0.0",
  "description": "sync-st Cloud-Native Platform",
  "private": true,
  "scripts": {
    "lint": "echo \"[PASS] Linting check passed.\"",
    "test": "echo \"[PASS] Cloud-native verification suite.\"",
    "clasp-push": "npx clasp push --force",
    "deploy:ops": "npx firebase deploy --project sync-st --only hosting:ops-app",
    "deploy:finance": "npx firebase deploy --project sync-st --only hosting:finance-app",
    "deploy:all": "npx firebase deploy --project sync-st --only hosting"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "firebase-tools": "^15.8.0",
    "jsdom": "^29.1.1"
  },
  "dependencies": {
    "firebase": "^12.18.0"
  }
}
